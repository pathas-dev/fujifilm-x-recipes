const Loading = () => {
  return (
    <main className="w-full h-[calc(100vh-4rem)] flex items-center justify-center">
      <span className="loading loading-bars loading-lg m-auto" />
    </main>
  );
};
export default Loading;
